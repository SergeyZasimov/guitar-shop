export enum RouteDomain {
  Auth = 'auth',
  Product = 'products',
  Comment = 'comments',
  Order = 'orders',
}

export enum RoutePath {
  Register = 'register',
  Login = 'login',
  CheckStatus = 'check-status',
}
